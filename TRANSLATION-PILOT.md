# 翻譯方案小規模測試

測試日期：2026-09-04。只驗證可行性，尚未整合進教練。

## 方法與範圍

- 在既有瀏覽器工作階段中，另開背景 Google 翻譯頁面，選英文 → 中文（繁體）。
- 以介面操作輸入 3 個不同的遊戲情境短句，另重複其中 1 句。讀取頁面實際顯示的譯文。
- 未呼叫未公開的翻譯端點、未建立 API 金鑰／付費服務、未錄音、未讀取遊戲記憶體。
- 本次沒有出現驗證、封鎖或限流；4 次成功回傳不代表長期穩定性或無限額度。

## 觀察結果

| 測試英文 | 頁面顯示的繁中 | 判讀 |
|---|---|---|
| Head to Guard's Watch. | 前往守衛崗哨。 | 行動意思可用；地名被翻譯，仍應並列英文避免找不到目的地。 |
| This effect increases the damage dealt by your summons. | 此效果會增加你的召喚物造成的傷害。 | 在此句中可用。 |
| I need your help. Follow me and stay close. | 我需要你的幫助。 跟著我，跟我保持聯絡。 | stay close 在此處是保持靠近，不是保持聯絡；需要情境校正。 |
| 重複第 2 句 | 同第 2 句 | 成功，可能受網站端／工作階段快取影響。 |

最後一筆從開始替換輸入至已知譯文可見約 **479 ms**，包含瀏覽器控制往返，不是純 Google 伺服器延遲，也不能代表第一次翻譯速度。前三筆結果跨越多次人工代理觀察，沒有可信的精確端到端計時，不計算平均值。

本次未量測獨立瀏覽器的增量記憶體、遊戲 FPS、长時間限流或完整「OCR → 翻譯 → 朗讀」延遲。測試依賴目前可用的瀏覽器控制環境，尚不是可由教練 EXE 獨立執行的翻譯後端。

## 結論

可作為免費方案的原型方向，但不宜將 4 次成功當成正式上線驗收。接下來需獨立整合瀏覽器生命週期、翻譯完成判定、每次只送一個新句子、快取／去重、錯誤退避與遊戲詞彙校正。遇到驗證／限流就暫停，不繞過、不無限重試。原英文應始終保留。

因此沒有把網頁自動化放進正式程式：頁面結構、驗證與瀏覽器生命週期都不是可靠 API，也會額外占用遊戲電腦資源。

## 本機 Argos Translate 小規模測試

同日另以 [Argos Translate](https://github.com/argosopentech/argos-translate) `translate-en_zh-1_9`（OPUS-MT／CTranslate2）測試，模型約 85 MB，程序尖峰記憶體約 172 MiB。這台電腦使用 1 個 CPU 執行緒時單句約 161～331 ms，4 個執行緒約 98～225 ms；速度足以當本機短句翻譯，但 Diablo 語境品質不合格。

| 英文詞句 | 本機結果 | 問題 |
|---|---|---|
| Head Forward and Search for Leoric | 頭頭前行並尋找獅子座 | 人名被誤認成星座，任務不可用 |
| Mad King's Breach | 瘋狂的國王的破壞 | 地名失真 |
| summons | 傳票 | 遊戲中應是召喚物 |
| shard | 堅硬之人 | 遊戲中應是碎片 |
| skill | 技術 | 遊戲 UI 應是技能 |

較大的 [NLLB-200 distilled 600M](https://huggingface.co/facebook/nllb-200-distilled-600M) 權重約 2.46 GB（float32），模型卡為 CC-BY-NC 4.0；對這台同時執行遊戲的筆電而言成本較高，也不適合作為這個可公開發布專案的預設翻譯元件。Argos 測試模型與套件保留在 git 忽略的 `.local-translation`，不進發布包或 GitHub。

## 正式採用方式：Ollama 小模型

考量 Azure 設定成本後，主流程改為 [Ollama `qwen3.5:0.8b`](https://ollama.com/library/qwen3.5:0.8b)，Azure 只保留為選用備援。模型約 1 GB，不需帳號或金鑰，請求只到 `127.0.0.1`；常見任務與已知易錯句先由本機規則直接處理，其他句子使用固定 Diablo 詞彙表提示與輸出校正。

本機實測：第一次冷載入約 4.7 秒；保持載入且使用 4 個 CPU 執行緒時，代表性短句約 0.9～1.5 秒。戰鬥忙碌模式刻意限制為 1 個執行緒，一句新測試約 5.3 秒，換取較低遊戲干擾；對話期間通常沒有持續戰鬥鍵，會回到較快的執行緒預算。`Defeat the undead and find the shard`、護盾／生命值、比較裝備、技能冷卻等句子可用；0.8B 對 `I need your help. Follow me and stay close.` 曾產生不自然譯文，因此這個常見句已改由固定規則處理。重複句從快取讀取不到 1 ms。這不是完整品質基準，遊戲專名與少見長句仍可能需要看英文原文核對。

譯文與 2B 教練的決策各自保存本機快取。自動測試以假的 HTTP 回應驗證 loopback、模型、詞彙校正、快取及 Azure 備援；另用已安裝的真實 0.8B 模型進行上述小規模端到端測試。

## 本機攻略更新實測

實際更新器讀取固定公開攻略來源、解析有限欄位並保存本機快取，本次約 **822 ms**。沒有 LLM 呼叫或遊戲文字上傳。來源為 [Dexerto 死靈法師 PvE 建議](https://www.dexerto.com/diablo/best-diablo-immortal-necromancer-builds-1870129/)，作者更新日期仍是 **2026-06-17**；今天下載並不使其變成今天的版本攻略。

已建立自動檢查、來源日期、舊資料標示、最後成功快取與備份復原。這是資料快取原型，不是完整攻略資料庫，也沒有驗證當季最強配置。只抽取一份來源的技能／裝備候選，未自動比對全部官方補丁、遊戲實裝狀態或其他作者結論。

舊版配裝提示原本就是固定文字，沒有額外 LLM 呼叫；這次改善的是可查證資料與更新能力，不是直接減少翻譯模型呼叫次數。
